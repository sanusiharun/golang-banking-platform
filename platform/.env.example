# platform/.env — shared platform services
# Copy to platform/.env and fill in real values.
# ⚠️  Never commit the real .env file.

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=redispassword
